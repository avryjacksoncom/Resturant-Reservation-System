spring.application.name=fullstack-backend
spring.datasource.url=jdbc:mysql://localhost:3306/restuarant_db
# I changed the name of the database from resturantdb to restDataBase
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


# # Hibernate settings
# spring.jpa.hibernate.ddl-auto=create  # Will recreate tables every time the app starts
# spring.jpa.show-sql=true  # Print SQL queries to console for debugging
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect